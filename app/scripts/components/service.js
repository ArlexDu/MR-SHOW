'use strict';

app.component(
    'service', {
        templateUrl: 'views/service.html',
        controllerAs: 'service',
        controller: function ($state) {
            var self = this;


            self.$onInit = function() {
            };

        }
    }
);
